#include <iostream>
#include <lemon/list_graph.h>
#include <queue>
#include <set>
#include <fstream>
#include <lemon/lgf_writer.h>
#include <lemon/concepts/ugraph.h>

using namespace lemon;

int main (void)
{
  ListGraph g;
  ListGraph::Node a = g.addNode();
  for(ListGraph::OutEdgeIt e(g,a); e!=INVALID; ++e);


  return 0;
}
